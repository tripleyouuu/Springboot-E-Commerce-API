package com.shopping.Ecommerce.model;

/**
 * Order status values as per assignment:
 * CREATED, PAID, FAILED, CANCELLED
 */
public enum OrderStatus {
    CREATED,
    PAID,
    FAILED,
    CANCELLED
}
